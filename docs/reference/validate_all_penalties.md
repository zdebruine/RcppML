# Validate all penalties at once

Validate all penalties at once

## Usage

``` r
validate_all_penalties(L1, L2, L21, angular, graph_lambda, upper_bound)
```

## Arguments

- L1, L2, L21, angular, graph_lambda, upper_bound:

  Penalty parameters to validate

## Value

Named list of validated length-2 penalty vectors
