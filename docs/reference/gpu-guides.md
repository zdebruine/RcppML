# GPU-Accelerated Post-Hoc Guide Methods

GPU implementations of embedding correction methods using CUDA kernels
with cuBLAS/cuSOLVER. All methods accept an n x k embedding matrix and
return the corrected embedding.
