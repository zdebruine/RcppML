# Decompress a SparsePress raw vector to a dgCMatrix

Decompress a SparsePress raw vector to a dgCMatrix

## Usage

``` r
Rcpp_sp_decompress(data)
```

## Arguments

- data:

  A raw vector containing compressed SparsePress data

## Value

A sparse matrix (dgCMatrix)
