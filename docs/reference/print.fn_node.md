# Print an fn_node

Print an fn_node

## Usage

``` r
# S3 method for class 'fn_node'
print(x, ...)
```

## Arguments

- x:

  An `fn_node` object.

- ...:

  Additional arguments (unused).

## Value

Invisibly returns `x`.
