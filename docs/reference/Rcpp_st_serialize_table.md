# Serialize a data.frame to obs/var table binary format

Serialize a data.frame to obs/var table binary format

## Usage

``` r
Rcpp_st_serialize_table(df)
```

## Arguments

- df:

  A data.frame

## Value

A raw vector containing the serialized table
