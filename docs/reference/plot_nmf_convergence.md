# Plot Convergence Analysis

Plot Convergence Analysis

## Usage

``` r
plot_nmf_convergence(x, smooth, span, interactive, theme)
```

## Value

A `ggplot2` object (or `plotly` if `interactive = TRUE`)
