% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guides.R
\name{guide_callback}
\alias{guide_callback}
\title{Create a callback guide}
\usage{
guide_callback(fn, lambda = 1, side = c("H", "W"))
}
\arguments{
\item{fn}{A function with signature \code{fn(factor, iter)} that returns
a target matrix (k x n for H, k x m for W).}

\item{lambda}{Guide strength. Positive attracts; negative repels. Default 1.0.}

\item{side}{Which factor to guide: "H" (default) or "W".}
}
\value{
An \code{nmf_guide} object.
}
\description{
Steers a factor toward a target computed by a user-supplied R function.
The function is called each ALS iteration with the current factor matrix
and iteration number, and must return a target matrix of matching
dimensions.
}
\examples{
# Guide that steers factors toward a decaying target
g <- guide_callback(
  fn = function(factor, iter) factor * exp(-0.01 * iter),
  lambda = 1.0
)
}
\seealso{
\code{\link{guide_classifier}}, \code{\link{guide_external}}, \code{\link{nmf}}
}
