% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsepress.R
\name{sp_read}
\alias{sp_read}
\title{Read a SparsePress file into a dgCMatrix}
\usage{
sp_read(path, cols = NULL, reorder = TRUE)
}
\arguments{
\item{path}{Path to a \code{.spz} file.}

\item{cols}{Optional integer vector of column indices to read (1-indexed).
For v2 files, enables partial reads by only decoding the necessary chunks.
Should be a contiguous range, e.g. \code{1:500}.}

\item{reorder}{Logical; if \code{TRUE} (default), undo any row permutation
that was applied during compression (v2 only).}
}
\value{
A \code{dgCMatrix} sparse matrix.
}
\description{
Read a SparsePress file into a dgCMatrix
}
\examples{
\donttest{
library(Matrix)
A <- rsparsematrix(100, 50, 0.1)
f <- tempfile(fileext = ".spz")
sp_write(A, f)
B <- sp_read(f)
all.equal(A, B)
unlink(f)
}

}
\seealso{
\code{\link{sp_write}}, \code{\link{sp_info}}
}
