% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streampress.R
\name{st_write}
\alias{st_write}
\title{Write a sparse matrix to a StreamPress file}
\usage{
st_write(
  x,
  path,
  delta = TRUE,
  value_pred = FALSE,
  verbose = FALSE,
  precision = "auto",
  row_sort = FALSE,
  include_transpose = FALSE,
  chunk_cols = 2048L
)
}
\arguments{
\item{x}{A sparse matrix (\code{dgCMatrix}) or object coercible to one.}

\item{path}{Output file path. Extension \code{.spz} is recommended.}

\item{delta}{Logical; use density-based delta prediction for structure.
Default \code{TRUE}.}

\item{value_pred}{Logical; use value prediction for integer-valued data.
Default \code{FALSE}.}

\item{verbose}{Logical; print compression statistics. Default \code{FALSE}.}

\item{precision}{Value precision: \code{"auto"} (default), \code{"fp32"},
\code{"fp16"}, \code{"quant8"}, \code{"fp64"}.}

\item{row_sort}{Logical; sort rows by nnz for better compression.}

\item{include_transpose}{Logical; store CSC(A^T) in the file.}

\item{chunk_cols}{Integer; columns per chunk. Default 2048.}
}
\value{
Invisibly returns a list with compression statistics.
}
\description{
Write a sparse matrix to a StreamPress file
}
\examples{
\donttest{
library(Matrix)
A <- rsparsematrix(1000, 500, 0.05)
f <- tempfile(fileext = ".spz")
st_write(A, f)
B <- st_read(f)
all.equal(A, B)  # TRUE
unlink(f)
}

}
\seealso{
\code{\link{st_read}}, \code{\link{st_info}}
}
