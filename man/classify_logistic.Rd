% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifier_metrics.R
\name{classify_logistic}
\alias{classify_logistic}
\title{Logistic regression classifier for factor embeddings}
\usage{
classify_logistic(
  embedding,
  labels,
  test_fraction = 0.2,
  test_idx = NULL,
  seed = NULL
)
}
\arguments{
\item{embedding}{Numeric matrix where rows are samples and columns are
features (e.g., \code{t(result$H)} for sample embeddings).}

\item{labels}{Integer or factor vector of class labels. Length must equal
\code{nrow(embedding)}.}

\item{test_fraction}{Fraction of samples held out for testing (default 0.2).}

\item{test_idx}{Optional integer vector of test indices. If provided,
\code{test_fraction} is ignored.}

\item{seed}{Random seed for train/test split reproducibility.}
}
\value{
An \code{fn_classifier_eval} object (same structure as KNN variant).
}
\description{
Trains a multinomial logistic regression on factor embeddings using
\code{stats::glm} (one-vs-rest for > 2 classes) and evaluates on test
samples with the same comprehensive metrics as \code{classify_embedding}.
}
\examples{
\donttest{
# Logistic regression on random embeddings
set.seed(42)
embed <- matrix(rnorm(200), nrow = 40, ncol = 5)
labels <- factor(rep(1:4, each = 10))
eval <- classify_logistic(embed, labels, seed = 1)
eval$accuracy
}
}
\seealso{
\code{\link{classify_embedding}}, \code{\link{classify_rf}}
}
