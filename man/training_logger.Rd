% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_log.R
\name{training_logger}
\alias{training_logger}
\title{Create a training logger for factor network fitting}
\usage{
training_logger(
  log_loss = TRUE,
  log_norms = FALSE,
  log_classifier = NULL,
  interval = 1L
)
}
\arguments{
\item{log_loss}{Log reconstruction loss per layer (default TRUE).}

\item{log_norms}{Log factor W/H norms per layer (default FALSE).}

\item{log_classifier}{Evaluate classifier accuracy per iteration
using a supplied \code{classify_embedding} configuration.
Must be a list with \code{labels} and optionally \code{test_idx},
\code{k}, \code{side}, and \code{layer} (default NULL = disabled).}

\item{interval}{Log every \code{interval} iterations (default 1).}
}
\value{
A \code{training_logger} object to pass to \code{fit()}.
}
\description{
Returns a logger object that records per-iteration metrics during
\code{fit()}. After training, the log can be printed, plotted, or
exported to CSV.
}
\examples{
\dontrun{
logger <- training_logger(
  log_norms = TRUE,
  log_classifier = list(labels = labels, test_idx = test_idx, k = 5)
)
res <- fit(net, logger = logger)
print(logger)
plot(logger)
export_log(logger, "training_log.csv")
}
}
\seealso{
\code{\link{nmf}}, \code{\link{fit}}
}
