% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulateNMF.R
\name{simulateNMF}
\alias{simulateNMF}
\title{Simulate an NMF dataset}
\usage{
simulateNMF(nrow, ncol, k, noise = 0.5, dropout = 0, seed = NULL)
}
\arguments{
\item{nrow}{number of rows (features)}

\item{ncol}{number of columns (samples)}

\item{k}{true rank (number of factors)}

\item{noise}{noise level as a multiplier on the mean signal. A value of 1.0 means the noise
standard deviation equals the mean signal value. Default: 0.5.}

\item{dropout}{fraction of entries to set to zero (0 = no dropout). Default: 0.}

\item{seed}{seed for random number generation}
}
\value{
list of dense matrix \code{A} and true \code{w} and \code{h} models
}
\description{
Generate a random nonnegative matrix with known factor structure for benchmarking NMF recovery.
Uses block-diagonal construction: each factor owns a disjoint subset of features (rows) and dominates
a disjoint subset of samples (columns), with small cross-talk for realism. This produces clearly
recoverable factors even at moderate noise levels. Inspired by \code{NMF::syntheticNMF}.
}
\examples{
data <- simulateNMF(50, 30, k = 3, noise = 0.1, seed = 42)
dim(data$A)  # 50 x 3
dim(data$w)  # 50 x 3
dim(data$h)  # 3 x 30

}
\seealso{
\code{\link{simulateSwimmer}}, \code{\link{nmf}}
}
