% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streampress.R
\name{st_write_list}
\alias{st_write_list}
\title{Write a List of Matrices as a Single StreamPress File}
\usage{
st_write_list(
  x,
  path,
  obs = NULL,
  var = NULL,
  chunk_bytes = 6.4e+07,
  chunk_cols = NULL,
  include_transpose = TRUE,
  precision = "auto",
  threads = 0L,
  verbose = FALSE
)
}
\arguments{
\item{x}{A list of \code{dgCMatrix} objects (or coercible).
All must have identical \code{nrow}.}

\item{path}{Output \code{.spz} path.}

\item{obs}{Optional data.frame of cell metadata (\code{nrow} == total cols).}

\item{var}{Optional data.frame of gene metadata (\code{nrow} == nrow of mats).}

\item{chunk_bytes}{Target bytes per chunk. Default 64 MB.}

\item{chunk_cols}{Explicit column count per chunk. Overrides \code{chunk_bytes}.}

\item{include_transpose}{Logical. Default \code{TRUE}.}

\item{precision}{Value precision. Default \code{"auto"}.}

\item{threads}{Integer. 0 = all threads.}

\item{verbose}{Logical.}
}
\value{
Invisibly, compression statistics.
}
\description{
Column-concatenates a list of matrices and writes them as a single
\code{.spz} file. All matrices must have the same number of rows.
}
\examples{
\dontrun{
mats <- list(mat1, mat2)
st_write_list(mats, "combined.spz")
}

}
\seealso{
\code{\link{st_write}}
}
