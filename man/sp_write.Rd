% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsepress.R
\name{sp_write}
\alias{sp_write}
\title{Write a sparse matrix to a SparsePress file}
\usage{
sp_write(
  x,
  path,
  delta = TRUE,
  value_pred = FALSE,
  verbose = FALSE,
  precision = "auto",
  row_sort = FALSE,
  include_transpose = FALSE
)
}
\arguments{
\item{x}{A sparse matrix (\code{dgCMatrix}) or object coercible to one.}

\item{path}{Output file path. Extension \code{.spz} is recommended.}

\item{delta}{Logical; use density-based delta prediction for structure.
Default \code{TRUE}. Improves compression for structured sparsity patterns.}

\item{value_pred}{Logical; use value prediction for integer-valued data.
Default \code{FALSE}. Only effective if all values are non-negative integers.}

\item{verbose}{Logical; print compression statistics. Default \code{FALSE}.}

\item{precision}{Value precision for v2 format: \code{"auto"} (default),
\code{"fp32"}, \code{"fp16"}, \code{"quant8"}, \code{"fp64"}.}

\item{row_sort}{Logical; sort rows by nnz for better compression (v2 only).}

\item{include_transpose}{Logical; store CSC(A^T) in the file (v2 only).
Approximately doubles file size but eliminates runtime transpose cost.}
}
\value{
Invisibly returns a list with compression statistics:
  \code{raw_bytes}, \code{compressed_bytes}, \code{ratio}, \code{compress_ms}.
}
\description{
Write a sparse matrix to a SparsePress file
}
\examples{
\donttest{
library(Matrix)
A <- rsparsematrix(1000, 500, 0.05)
f <- tempfile(fileext = ".spz")
sp_write(A, f)
B <- sp_read(f)
all.equal(A, B)  # TRUE
unlink(f)
}

}
\seealso{
\code{\link{sp_read}}, \code{\link{sp_info}}
}
