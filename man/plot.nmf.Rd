% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_nmf.R
\name{plot.nmf}
\alias{plot.nmf}
\title{Plot NMF Training History and Diagnostics}
\usage{
\method{plot}{nmf}(
  x,
  type = c("loss", "convergence", "regularization", "sparsity"),
  smooth = TRUE,
  span = 0.3,
  log_scale = FALSE,
  interactive = FALSE,
  theme = "classic",
  ...
)
}
\arguments{
\item{x}{object of class "nmf"}

\item{type}{plot type: 
- "loss": Loss components over iterations (default)
- "convergence": Log-scale loss convergence
- "regularization": Regularization penalty contributions
- "sparsity": Factor sparsity patterns}

\item{smooth}{apply smoothing (LOESS) for noisy curves (default TRUE)}

\item{span}{smoothing span for LOESS (default 0.3)}

\item{log_scale}{use log scale for y-axis (default FALSE, auto TRUE for "convergence")}

\item{interactive}{create interactive plotly plot (default FALSE)}

\item{theme}{ggplot2 theme: "classic", "minimal", "dark" (default "classic")}

\item{...}{additional arguments passed to specific plotting functions}
}
\value{
ggplot2 or plotly object
}
\description{
TensorBoard-style visualization of NMF training dynamics, convergence analysis,
and factor diagnostics. Provides multiple plot types for comprehensive model analysis.
}
\examples{
\donttest{
# Basic loss plot
model <- nmf(hawaiibirds, k = 10)
plot(model)

# Convergence analysis
plot(model, type = "convergence")

# Interactive plot
plot(model, type = "loss", interactive = TRUE)

# Compare multiple runs
models <- replicate(5, nmf(hawaiibirds, k = 10), simplify = FALSE)
plot(models[[1]], type = "sparsity")
}
}
\seealso{
\code{\link{nmf}}, \code{\link{compare_nmf}}
}
